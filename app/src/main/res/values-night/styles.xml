<resources>

    <style name="AppTheme" parent="Theme.Dark"/>

    <style name="ToolbarPopup" parent="ThemeOverlay.Material3.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="NumberPicker">
        <item name="bpDialogBackground">@drawable/background_primary</item>
        <item name="bpDividerColor">@color/background_dark_tertiary</item>
        <item name="bpTextColor">@color/text_primary</item>
        <item name="bpDeleteIcon">@drawable/ic_backspace_dark</item>
        <item name="bpCheckIcon">@drawable/ic_check_dark</item>
    </style>

</resources>