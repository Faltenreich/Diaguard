CREATE TABLE entry (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    dateTime TEXT NOT NULL,
    note TEXT
);

selectAll:
SELECT * FROM entry;

insert:
INSERT INTO entry(dateTime, note)
VALUES (?, ?);

delete:
DELETE FROM entry
WHERE id = ?;